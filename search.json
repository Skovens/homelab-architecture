{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Homelab Architecture","text":"<p>I run a homelab. Not just services on a server — an architecture. Every technology choice, every configuration, every deployment pattern is intentional. This site documents what I built, how I built it, and most importantly, why.</p>","path":["Home"],"tags":[]},{"location":"#why-this-exists","level":2,"title":"Why This Exists","text":"<p>I transitioned from industrial automation to infrastructure engineering. The homelab is where I practice, experiment, and prove that I can design, deploy, and maintain production-grade systems. This site is both a portfolio and a reference — showing my thinking, not just my configs.</p>","path":["Home"],"tags":[]},{"location":"#hardware","level":2,"title":"Hardware","text":"","path":["Home"],"tags":[]},{"location":"#home-server","level":3,"title":"Home Server","text":"Component Spec CPU AMD Ryzen 5 3600 Board ASRockRack X570D4U RAM 128 GB DDR4-3200 GPU NVIDIA Tesla P40 (24 GB VRAM) OS Debian 13 Storage ZFS (<code>bulkdata/</code> pool)","path":["Home"],"tags":[]},{"location":"#disaster-recovery-host","level":3,"title":"Disaster Recovery Host","text":"Component Spec Board RockPro64 (ARM64, RK3399) OS FreeBSD 15.0 (ZFS root) Storage <code>zpool/backup</code>","path":["Home"],"tags":[]},{"location":"#reverse-proxy","level":3,"title":"Reverse Proxy","text":"Component Spec OS Debian 13 (VPS)","path":["Home"],"tags":[]},{"location":"#how-to-read-this-site","level":2,"title":"How to Read This Site","text":"<p>Each section follows the same pattern:</p> <ol> <li>What I'm solving</li> <li>How I implemented it</li> <li>Why this way, not the obvious alternative</li> </ol> <p>Code samples are concrete but redacted — real config patterns, sanitized paths and domains. The Decisions section contains architecture decision records explaining the reasoning behind each major choice.</p>","path":["Home"],"tags":[]},{"location":"philosophy/","level":1,"title":"Philosophy","text":"<p>Three principles drive every decision in this homelab.</p>","path":["Philosophy"],"tags":[]},{"location":"philosophy/#rootless-first","level":2,"title":"Rootless-First","text":"<p>Containers run rootless. Services run as dedicated system users with <code>nologin</code> shells. There is no shared service account. A container escape gives the attacker a single UID with no shell, no home directory content, and no access to other services' secrets.</p> <p>This is not the default for most homelab guides. Most use Docker with root, or a single <code>podman</code> user for everything. I chose the harder path because the security model is sound: blast radius is per-service, not per-host.</p>","path":["Philosophy"],"tags":[]},{"location":"philosophy/#infrastructure-as-code","level":2,"title":"Infrastructure as Code","text":"<p>Every service is deployed by Ansible. No manual <code>podman run</code>. No SSH-in-and-fix. If it's not in the repo, it doesn't exist on the server.</p> <p>This means I can rebuild the entire homelab from scratch. It means changes are reviewed, versioned, and auditable. It means I can show you exactly how every service is configured — and you can reproduce it.</p>","path":["Philosophy"],"tags":[]},{"location":"philosophy/#intentional-simplicity","level":2,"title":"intentional Simplicity","text":"<p>I use Podman instead of Docker. Quadlets instead of Compose. Sanoid instead of a backup script. Each choice favors the tool that does one thing well, integrates with the system natively, and doesn't require a daemon running as root.</p> <p>I don't run Prometheus, Grafana, or Loki. I run healthchecks.io. Monitoring should tell me if something is down, not give me 400 dashboards I never look at.</p> <p>I don't run a full Kubernetes cluster. I run systemd with quadlets. For 15 services, Kubernetes is overhead, not capability.</p> <p>Every technology in this homelab earned its place. Nothing is here because it's popular — it's here because it solves a specific problem better than the alternative.</p>","path":["Philosophy"],"tags":[]},{"location":"ai-stack/","level":1,"title":"AI Stack","text":"<p>The AI stack runs entirely on-premises. A Tesla P40 GPU, llama.cpp for inference, Open WebUI as the frontend, and a constellation of MCP servers for tool integration.</p> <ul> <li>GPU — Tesla P40, NVIDIA R580 driver</li> <li>Inference Servers — llama.cpp GPU vs CPU, MTP speculative decoding</li> <li>Model Proxy — llama-swap FIFO scheduler</li> <li>Open WebUI — frontend wired via IaC</li> <li>Document Processing — docling-serve</li> <li>MCP Servers — GitHub, Forgejo, Vault tool servers</li> <li>Knowledge Base — oikb, Obsidian vault sync</li> </ul>","path":["AI Stack"],"tags":[]},{"location":"ai-stack/document-processing/","level":1,"title":"Document Processing","text":"","path":["AI Stack","Document Processing"],"tags":[]},{"location":"ai-stack/document-processing/#what","level":2,"title":"What","text":"<p>Docling-serve converts documents (PDFs, images, text files) into structured data for Open WebUI's RAG pipeline. It runs as a CPU-only container.</p>","path":["AI Stack","Document Processing"],"tags":[]},{"location":"ai-stack/document-processing/#how","level":2,"title":"How","text":"<p>The container runs <code>quay.io/docling-project/docling-serve-cpu</code> — the lightweight variant without LibreOffice or ffmpeg.</p> <p>Key configuration:</p> Setting Value Reason <code>DOCLING_SERVE_MAX_SYNC_WAIT</code> 600 Supports large PDFs (13 MB+) <code>DOCLING_SERVE_ENG_LOC_NUM_WORKERS</code> 4 Conversion throughput <code>UVICORN_WORKERS</code> 1 Multiple workers cause \"Task Not Found\" errors <p>Open WebUI is configured to use docling as its extraction engine:</p> <pre><code>Environment=CONTENT_EXTRACTION_ENGINE=docling\n</code></pre>","path":["AI Stack","Document Processing"],"tags":[]},{"location":"ai-stack/document-processing/#why","level":2,"title":"Why","text":"<p>Docling was chosen over alternatives (Unstructured, LlamaParse) because:</p> <ul> <li>Self-hosted — no data leaves the network</li> <li>CPU-only image available — no GPU required for document processing</li> <li>Good PDF support — handles tables, images, and complex layouts</li> <li>Active development — the docling project is actively maintained</li> </ul> <p>The <code>UVICORN_WORKERS=1</code> setting is a gotcha. Multiple workers cause \"Task Not Found\" errors with async conversion requests. This is a docling-serve limitation, not a configuration error. The docs recommend staying at 1 worker.</p> <p>Supported file types</p> <p>The docling-serve-cpu image supports PDF, images, and plain text. Legacy Office formats (<code>.doc</code>, <code>.xls</code>, <code>.ppt</code>) require LibreOffice (not included). Audio/video require ffmpeg (not included). See ADR-0006 for the file type allowlist.</p>","path":["AI Stack","Document Processing"],"tags":[]},{"location":"ai-stack/gpu/","level":1,"title":"GPU","text":"","path":["AI Stack","GPU"],"tags":[]},{"location":"ai-stack/gpu/#what","level":2,"title":"What","text":"<p>An NVIDIA Tesla P40 provides 24 GB VRAM for LLM inference. It runs the R580 driver via the <code>.run</code> installer — the only way to get CUDA 12.8-compatible drivers on Debian 13 for a Pascal GPU.</p>","path":["AI Stack","GPU"],"tags":[]},{"location":"ai-stack/gpu/#how","level":2,"title":"How","text":"<p>The Tesla P40 is a datacenter GPU — no display output, passive cooling, 250W TDP. It sits in an ASRockRack X570D4U board with a Ryzen 3600 and 128 GB DDR4-3200.</p> <p>Driver installation:</p> <ul> <li>NVIDIA R580 (<code>NVIDIA-Linux-x86_64-580.173.02.run</code>) via <code>.run</code> installer with DKMS</li> <li><code>nvidia-container-toolkit</code> from official repo</li> <li>CDI configured for rootless Podman: <code>nvidia-ctk config --cdi-default --no-cgroups</code></li> <li>APT pinned to <code>580*</code> to prevent accidental upgrades</li> </ul> <p>GPU management tools:</p> <ul> <li><code>nvidia-pstated</code> — power state management</li> <li><code>gpu-fancontrol</code> — IPMI-based fan control (passive card needs external cooling)</li> <li><code>gpu-night-mode</code> — scheduled power limit (125W) for quiet nighttime operation</li> </ul>","path":["AI Stack","GPU"],"tags":[]},{"location":"ai-stack/gpu/#why","level":2,"title":"Why","text":"<p>The Tesla P40 was chosen for three reasons:</p> <ol> <li>24 GB VRAM — enough for 7B-27B parameter models at full precision</li> <li>Pascal architecture — well-supported by llama.cpp, mature CUDA stack</li> <li>Price — datacenter decommissioning means P40s are available for ~$100-150</li> </ol> <p>The driver situation is the main complication. NVIDIA dropped Pascal support from R590+, and Debian 13 only ships R550 (too old for CUDA 12.8 containers). The <code>.run</code> installer is the only path to R580 on Debian 13. This is documented in ADR-0008.</p> <p>R580 is a dead end</p> <p>R580 is the last driver to support Pascal. If the P40 dies, the replacement GPU must be Ampere or newer (which supports R590+). The entire driver installation process changes for newer GPUs.</p>","path":["AI Stack","GPU"],"tags":[]},{"location":"ai-stack/inference-servers/","level":1,"title":"Inference Servers","text":"","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#what","level":2,"title":"What","text":"<p>llama.cpp runs twice — once on GPU for fast inference, once on CPU for a dense 256K-context model that doesn't fit in VRAM. Both run as rootless containers in the AI services pod.</p>","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#how","level":2,"title":"How","text":"","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#gpu-instance-port-8090","level":3,"title":"GPU Instance (port 8090)","text":"<p>Runs <code>ghcr.io/ggml-org/llama.cpp:server-cuda12</code> with CUDA acceleration. Models:</p> Model Quant ctx-size Notes Gemma 4 MoE UD-Q6_K_XL 262144 cpu-moe, MTP external drafter Qwen 3.6 MoE UD-Q6_K_XL 262144 cpu-moe, f16 KV, internal MTP Qwen 3.8 Dense UD-Q3_K_XL 262144 q8_0 KV, internal MTP, ubatch=768","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#cpu-instance-port-8091","level":3,"title":"CPU Instance (port 8091)","text":"<p>Runs <code>ghcr.io/ggml-org/llama.cpp:server</code> (CPU-only, no CUDA). Serves the same Qwen 3.8 Dense model at Q3_K_XL quantization — text-only, no multimodal.</p>","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#key-techniques","level":3,"title":"Key Techniques","text":"<p>MTP (Multi-Token Prediction) Speculative Decoding — roughly doubles generation speed. The model predicts multiple tokens per forward pass, then verifies them in parallel. On the P40, this takes TG from ~15 t/s to ~31 t/s for Gemma 4.</p> <p>cpu-moe (Mixture of Experts on CPU) — routes MoE expert FFN layers to DDR4 while keeping attention on GPU. This allows larger MoE models to fit in 24 GB VRAM by offloading the sparse expert layers to system RAM.</p>","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#model-downloads","level":3,"title":"Model Downloads","text":"<p>Models are downloaded via the <code>hf</code> CLI (Hugging Face) installed under the <code>ai-services</code> user via <code>pipx</code>. Downloads are idempotent — <code>args.creates</code> prevents re-downloading existing files.</p>","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/inference-servers/#why","level":2,"title":"Why","text":"<p>Two instances because no single configuration handles both workloads:</p> <ul> <li>GPU instance: fast inference for interactive use (chat, code generation). Needs CUDA, needs VRAM.</li> <li>CPU instance: slow but capable inference for the dense 256K-context model. This model is too large for the P40's 24 GB VRAM at usable precision. On CPU it runs at ~2-4 t/s — slow, but functional for long-context tasks where latency doesn't matter.</li> </ul> <p>The cpu-moe technique is the key innovation: instead of running the entire MoE model on GPU (which would require 48+ GB VRAM), only the attention layers stay on GPU. The expert FFN layers — which are sparse and don't all activate for every token — run on CPU. This fits a 35B-parameter MoE model in 24 GB VRAM.</p>","path":["AI Stack","Inference Servers"],"tags":[]},{"location":"ai-stack/knowledge-base/","level":1,"title":"Knowledge Base","text":"","path":["AI Stack","Knowledge Base"],"tags":[]},{"location":"ai-stack/knowledge-base/#what","level":2,"title":"What","text":"<p>oikb syncs external sources into Open WebUI Knowledge Bases for RAG (Retrieval-Augmented Generation). Currently it syncs the Obsidian vault.</p>","path":["AI Stack","Knowledge Base"],"tags":[]},{"location":"ai-stack/knowledge-base/#how","level":2,"title":"How","text":"<p>oikb runs on port 8084 and pulls from a local directory. The Obsidian vault is a git repository cloned to the host, with an hourly git pull keeping it current.</p> <p>File type filtering restricts sync to docling-supported formats:</p> <pre><code>oikb_docling_filter: &amp;oikb_docling_filter\n  include_extensions:\n    - \".pdf\"\n    - \".md\"\n    - \".txt\"\n    - \".png\"\n    - \".jpg\"\n    - \".jpeg\"\n    - \".csv\"\n    - \".json\"\n</code></pre> <p>The YAML anchor (<code>&amp;oikb_docling_filter</code>) is shared across all sources — adding a new source automatically inherits the filter.</p>","path":["AI Stack","Knowledge Base"],"tags":[]},{"location":"ai-stack/knowledge-base/#why","level":2,"title":"Why","text":"<p>oikb bridges the gap between a knowledge repository (Obsidian vault) and the AI's context window. Without it, the AI has no access to my notes, documentation, or reference material.</p> <p>The file type filter is necessary because docling-serve-cpu cannot convert legacy Office formats or audio/video. Without the filter, sync would attempt conversion, fail, and retry every cycle.</p> <p>Forgejo connector pending</p> <p>A Forgejo connector for oikb is not yet available (oikb PR #12). When it lands, the vault will sync directly from Forgejo instead of a local git clone.</p>","path":["AI Stack","Knowledge Base"],"tags":[]},{"location":"ai-stack/mcp-servers/","level":1,"title":"MCP Servers","text":"","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/mcp-servers/#what","level":2,"title":"What","text":"<p>Three MCP (Model Context Protocol) servers give Open WebUI's AI agents access to external tools: GitHub, Forgejo (self-hosted git), and an Obsidian vault.</p>","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/mcp-servers/#how","level":2,"title":"How","text":"","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/mcp-servers/#github-mcp-port-8082","level":3,"title":"GitHub MCP (port 8082)","text":"<p>Runs via mcpo — a stdio-to-HTTP translation proxy. The GitHub MCP server is a stdio process; mcpo wraps it in an OpenAPI-compatible HTTP endpoint.</p> <p>GitHub PAT stored as a Podman secret.</p>","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/mcp-servers/#forgejo-mcp-port-8085","level":3,"title":"Forgejo MCP (port 8085)","text":"<p>Runs the native Forgejo MCP server with Streamable HTTP transport — no mcpo wrapper needed.</p> <p>Forgejo PAT stored as a Podman secret.</p>","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/mcp-servers/#vault-mcp-port-8095","level":3,"title":"Vault MCP (port 8095)","text":"<p>A custom build wrapping <code>@modelcontextprotocol/server-filesystem</code> + <code>supergateway</code> (stdio-to-HTTP). Mounts the Obsidian vault at <code>/vault</code> and exposes 8 MCP tools (read, write, edit, search).</p> <p>Published to <code>10.0.0.100:8095</code> for VPN access. No authentication — the VPN mesh is the trust boundary.</p>","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/mcp-servers/#why","level":2,"title":"Why","text":"<p>MCP servers give AI agents the ability to act on external systems. Instead of just chatting about code, the agent can:</p> <ul> <li>Read and write to GitHub repositories</li> <li>Search and modify Forgejo repositories</li> <li>Read and search the Obsidian vault for context</li> </ul> <p>The three transport patterns reflect the state of MCP server implementations:</p> <ul> <li>mcpo (GitHub): the GitHub MCP server only speaks stdio, so a translation layer is needed</li> <li>Native Streamable HTTP (Forgejo): the Forgejo MCP server speaks HTTP natively — simpler, fewer failure modes</li> <li>supergateway (Vault): the filesystem MCP server speaks stdio, so supergateway translates to HTTP</li> </ul> <p>Vault MCP has no auth</p> <p>The Vault MCP server has no authentication. Anyone on the VPN mesh can read and write to the Obsidian vault. This is acceptable because the VPN is the trust boundary — mesh enrollment requires a token, and all traffic is encrypted.</p>","path":["AI Stack","MCP Servers"],"tags":[]},{"location":"ai-stack/model-proxy/","level":1,"title":"Model Proxy","text":"","path":["AI Stack","Model Proxy"],"tags":[]},{"location":"ai-stack/model-proxy/#what","level":2,"title":"What","text":"<p>llama-swap sits in front of the llama.cpp instances and manages model switching. It prevents aborted in-flight streaming responses when Open WebUI switches models.</p>","path":["AI Stack","Model Proxy"],"tags":[]},{"location":"ai-stack/model-proxy/#how","level":2,"title":"How","text":"<p>llama-swap runs as a pure HTTP proxy (no GPU needed) on port 8092. It uses a FIFO scheduler:</p> <ol> <li>Open WebUI requests a model switch</li> <li>llama-swap queues the switch request</li> <li>Current streaming response completes</li> <li>llama-swap tells llama.cpp to unload the old model and load the new one</li> <li>New model is ready, queued request is served</li> </ol> <p>Configuration:</p> <pre><code>proxy: \"http://localhost:8090\"\nrouting:\n  router:\n    use: group\n  swap: true\n  exclusive: false\n</code></pre> <p>Model IDs in llama-swap must match the preset names in llama.cpp exactly.</p>","path":["AI Stack","Model Proxy"],"tags":[]},{"location":"ai-stack/model-proxy/#why","level":2,"title":"Why","text":"<p>Before llama-swap, Open WebUI switched models by telling llama.cpp to unload one while loading another. This aborted in-flight streaming responses — any user mid-conversation would see their response cut off.</p> <p>llama-swap's FIFO scheduler queues model-switch requests until the current stream drains. Users never see aborted responses.</p> <p>The proxy runs as <code>cmd: sleep infinity</code> in its quadlet — it's a long-running process managed by systemd, not a one-shot container. The actual proxy process is started as a second ExecStart in the quadlet.</p>","path":["AI Stack","Model Proxy"],"tags":[]},{"location":"ai-stack/open-webui/","level":1,"title":"Open WebUI","text":"","path":["AI Stack","Open WebUI"],"tags":[]},{"location":"ai-stack/open-webui/#what","level":2,"title":"What","text":"<p>Open WebUI is the frontend for all AI services. It connects to both llama.cpp instances, docling-serve for document processing, SearXNG for search, and multiple MCP servers for tool use.</p>","path":["AI Stack","Open WebUI"],"tags":[]},{"location":"ai-stack/open-webui/#how","level":2,"title":"How","text":"<p>Open WebUI is wired entirely via environment variables in the quadlet — no manual UI configuration:</p> <pre><code>[Container]\nImage=ghcr.io/open-webui/open-webui:main\nEnvironment=OPENAI_API_BASE_URLS=http://localhost:8090;http://8091\nEnvironment=ENABLE_PERSISTENT_CONFIG=false\n</code></pre> <p><code>ENABLE_PERSISTENT_CONFIG=false</code> forces environment variables to take precedence over UI settings. This means the Ansible playbook is the single source of truth for configuration.</p> <p>Tool servers are configured via <code>open_webui_tool_servers</code>:</p> Tool Protocol Endpoint GitHub MCP OpenAPI (via mcpo) <code>localhost:8082</code> Forgejo MCP Streamable HTTP <code>localhost:8085/mcp</code> Vault MCP Streamable HTTP <code>localhost:8095/mcp</code>","path":["AI Stack","Open WebUI"],"tags":[]},{"location":"ai-stack/open-webui/#why","level":2,"title":"Why","text":"<p>Open WebUI is not just a chat interface — it's the orchestration layer that ties together inference, document processing, search, and tool use. By wiring everything via environment variables, the entire stack is reproducible from the Ansible repo.</p> <p>The <code>ENABLE_PERSISTENT_CONFIG=false</code> setting is critical. Without it, changes made in the UI override the environment variables, and the Ansible repo no longer reflects the actual state. With it, every restart restores the IaC-defined configuration.</p> <p>Ordering constraint</p> <p>Open Terminal must run before Open WebUI in the playbook. Open Terminal creates an API key secret that Open WebUI reads at startup. If Open WebUI starts first, the secret doesn't exist yet.</p>","path":["AI Stack","Open WebUI"],"tags":[]},{"location":"backup-disaster-recovery/","level":1,"title":"Backup &amp; Disaster Recovery","text":"<p>Three layers of backup, one philosophy: never lose data.</p> <ul> <li>Three-Layer Strategy — snapshots, replication, cloud</li> <li>DR Host — FreeBSD on ARM, the recovery target</li> </ul>","path":["Backup & DR","Backup & Disaster Recovery"],"tags":[]},{"location":"backup-disaster-recovery/dr-host/","level":1,"title":"DR Host","text":"","path":["Backup & DR","DR Host"],"tags":[]},{"location":"backup-disaster-recovery/dr-host/#what","level":2,"title":"What","text":"<p>The disaster recovery host runs FreeBSD on a RockPro64 ARM board. It receives ZFS replication from the home server and serves as the recovery target.</p>","path":["Backup & DR","DR Host"],"tags":[]},{"location":"backup-disaster-recovery/dr-host/#how","level":2,"title":"How","text":"Component Spec Board RockPro64 (ARM64, RK3399) OS FreeBSD 15.0 Storage ZFS root (<code>zpool</code>) Firewall PF VPN DNClient (same mesh as home server) Fan control <code>fand</code> daemon for RockPro64 <p>The DR host runs its own Ansible playbook (<code>rockpro64.yml</code>) with roles for:</p> <ul> <li>ZFS import and configuration</li> <li>PF firewall</li> <li>DNClient mesh VPN</li> <li>Sanoid (for pruning replicated snapshots)</li> <li>Syncoid (for receiving replicated snapshots)</li> </ul>","path":["Backup & DR","DR Host"],"tags":[]},{"location":"backup-disaster-recovery/dr-host/#why","level":2,"title":"Why","text":"<p>FreeBSD on ARM was chosen for three reasons:</p> <ol> <li>ZFS maturity — FreeBSD's ZFS implementation is the reference. Linux ZFS (OpenZFS) is a port; FreeBSD ZFS is native.</li> <li>Low power — the RK3399 draws ~5W idle. The DR host runs 24/7, so power consumption matters.</li> <li>Hardware availability — the RockPro64 was already in my possession from previous projects.</li> </ol> <p>Performance ceiling</p> <p>The RK3399's network stack limits Nebula tunnel throughput to ~89 Mbps. This is a hardware limit — no software optimization can improve it. For a DR host that receives incremental ZFS snapshots, this is sufficient. For a primary server, it would not be.</p>","path":["Backup & DR","DR Host"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/","level":1,"title":"Three-Layer Backup Strategy","text":"","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/#what","level":2,"title":"What","text":"<p>Data is protected by three independent layers: ZFS snapshots, ZFS replication to a DR host, and cloud backup via rclone.</p>","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/#how","level":2,"title":"How","text":"","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/#layer-1-zfs-snapshots-sanoid","level":3,"title":"Layer 1: ZFS Snapshots (Sanoid)","text":"<p>On the home server, sanoid creates hourly/daily/weekly/monthly snapshots of critical datasets. Retention policy:</p> <ul> <li>24 hourly snapshots</li> <li>7 daily snapshots</li> <li>4 weekly snapshots</li> <li>6 monthly snapshots</li> </ul> <p>Snapshots are local — they protect against accidental deletion, corruption, or ransomware. They do not protect against hardware failure.</p>","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/#layer-2-zfs-replication-syncoid","level":3,"title":"Layer 2: ZFS Replication (Syncoid)","text":"<p>Every 2 hours, the DR host pulls snapshots from the home server via syncoid over SSH. The replication is pull-based — the DR host initiates, not the home server.</p> <p>This protects against hardware failure. If the home server dies, the DR host has a recent copy of all critical data.</p>","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/#layer-3-cloud-backup-rclone","level":3,"title":"Layer 3: Cloud Backup (Rclone)","text":"<p>Every 4 hours, rclone backs up data to pCloud. Two instances (per user) run as separate system users with independent credentials.</p> <p>This protects against site-wide disaster — fire, theft, flood. If the home and DR hosts are both lost, the cloud backup remains.</p>","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"backup-disaster-recovery/three-layer-strategy/#why","level":2,"title":"Why","text":"<p>Each layer covers a different failure mode:</p> Layer Protects against Recovery time RPO Snapshots Accidental deletion, corruption Minutes 1 hour Replication Hardware failure Hours 2 hours Cloud backup Site-wide disaster Hours to days 4 hours <p>No single layer is sufficient. Snapshots don't help if the disk is destroyed. Replication doesn't help if both hosts are in the same building. Cloud backup doesn't help if you need to recover a file deleted 30 minutes ago (it's 4 hours stale).</p> <p>RPO vs RTO</p> <p>RPO (Recovery Point Objective): how much data you can afford to lose. My RPO is 2 hours (the replication interval). RTO (Recovery Time Objective): how long it takes to restore service. For a file recovery, it's minutes (snapshot rollback). For a full server recovery, it's hours (rebuild from Ansible + restore from DR host).</p>","path":["Backup & DR","Three-Layer Strategy"],"tags":[]},{"location":"containerization/","level":1,"title":"Containerization","text":"<p>Every service in this homelab runs in a container. Not because containers are trendy — because they provide isolation, reproducibility, and declarative deployment.</p> <p>The containerization stack is built on four pillars:</p> <ul> <li>Podman over Docker — rootless, daemonless, secure by default</li> <li>Quadlets — systemd is the orchestrator, not Docker Compose</li> <li>Per-Service Users — nologin shells, UID isolation, separate secret stores</li> <li>The AI Services Pod — the one sanctioned exception to per-service isolation</li> <li>Egress Gate — fixing Podman's pasta boot-race networking problem</li> </ul>","path":["Containerization"],"tags":[]},{"location":"containerization/egress-gate/","level":1,"title":"Egress Gate","text":"","path":["Containerization","Egress Gate"],"tags":[]},{"location":"containerization/egress-gate/#what","level":2,"title":"What","text":"<p>Rootless Podman's default network mode (pasta) snapshots the host's network configuration at container start and never refreshes it. On a fresh boot, this means containers can start before the network is ready — and they stay broken until restarted.</p>","path":["Containerization","Egress Gate"],"tags":[]},{"location":"containerization/egress-gate/#how","level":2,"title":"How","text":"<p>A <code>wait-for-egress</code> script is deployed to every container user's <code>~/.local/bin/</code>. A systemd drop-in adds it as <code>ExecStartPre</code> to every container unit:</p> myapp.service.d/egress-gate.conf<pre><code>[Service]\nExecStartPre=/home/myapp/.local/bin/wait-for-egress\nTimeoutStartSec=150\nRestartSec=15\n</code></pre> <p>The script runs a bounded loop (default 120 seconds, 3-second interval) that checks:</p> <ol> <li>A default route exists</li> <li>A TCP probe succeeds (<code>1.1.1.1:443</code>)</li> <li>DNS resolves (<code>getent hosts github.com</code>)</li> </ol> <p>Only when all three pass does the script exit 0 and allow the container to start.</p> <p>The gate is implemented centrally in the <code>podman-container-base</code> role — every container role imports it, so new roles get the gate automatically. Opt-out per role via <code>pcb_egress_gate: false</code>.</p>","path":["Containerization","Egress Gate"],"tags":[]},{"location":"containerization/egress-gate/#why","level":2,"title":"Why","text":"<p>On 2026-08-24, the server booted at 15:02:33. Containers started at 15:02:50 — 17 seconds after boot. At that moment, the LAN and DNS had not settled. Every pasta instance snapshotted an unsettled network.</p> <p>The result: all container egress failed instantly. OpenHands couldn't reach its MCP endpoint. HomeAssistant lost weather data. The fix was simple — restart the affected services — but the root cause recurred on every reboot where networking took longer than ~15 seconds to settle.</p> <p>Restarting the affected user services forced fresh snapshots and fixed everything immediately. The repo history confirmed no networking misconfiguration — this is a pure boot-order race.</p> <p>Pasta doesn't refresh</p> <p>Pasta's <code>--config-net</code> snapshots host networking at start and never updates it. If the network changes after the container starts (e.g., DNS becomes available, default route appears), the container never sees it. This is by design in pasta — it's a feature for network isolation, but a problem for boot sequences.</p>","path":["Containerization","Egress Gate"],"tags":[]},{"location":"containerization/per-service-users/","level":1,"title":"Per-Service Users","text":"","path":["Containerization","Per-Service Users"],"tags":[]},{"location":"containerization/per-service-users/#what","level":2,"title":"What","text":"<p>Every containerized service runs under its own dedicated system user with a <code>nologin</code> shell. There is no shared service account.</p>","path":["Containerization","Per-Service Users"],"tags":[]},{"location":"containerization/per-service-users/#how","level":2,"title":"How","text":"<p>Each service user is created as a normal user (<code>system: no</code>) with <code>shell: /usr/sbin/nologin</code>:</p> <pre><code>- name: Create service user\n  user:\n    name: myservice\n    system: no\n    shell: /usr/sbin/nologin\n    create_home: yes\n    home: /home/myservice\n</code></pre> <p>Linger is enabled for each user so their systemd session persists without a login:</p> <pre><code>loginctl enable-linger myservice\n</code></pre> <p>Subuid/subgid ranges are auto-allocated by the system — never manually set with <code>usermod</code>.</p> <p>The per-service model:</p> User Services <code>ai-services</code> llamacpp, open-webui, docling-serve, searxng, oikb, MCP servers (shared pod) <code>healthchecks</code> healthchecks.io <code>forgejo</code> Forgejo git forge <code>jellyfin</code> Jellyfin media server <code>homeassistant</code> Home Assistant","path":["Containerization","Per-Service Users"],"tags":[]},{"location":"containerization/per-service-users/#why","level":2,"title":"Why","text":"<p>A shared service user means one UID for all containers. A compromise of any container gives the attacker that UID for every other container — no filesystem boundary, no secret isolation.</p> <p>Per-service users provide:</p> <ul> <li>UID isolation — container escape blast radius is limited to one user</li> <li>Separate secret stores — Podman secrets are scoped per user</li> <li>Separate audit trails — <code>journalctl --user --unit=myapp</code> shows only that service</li> <li>File ownership boundaries — each user owns only its own data</li> </ul> <p>The nologin shell matters</p> <p><code>nologin</code> prevents interactive access to the service user. Even if an attacker compromises the container and somehow gets host access, they cannot log in as that user. This is a defense-in-depth layer — the primary isolation comes from user namespaces (<code>UserNS=keep-id</code>), but the nologin shell removes another attack surface.</p> <p>Why <code>system: no</code>?</p> <p>Normal users get subuid/subgid auto-allocated. System users do not. Using <code>system: yes</code> would require manual <code>usermod</code> calls to set up subordinate UID/GID ranges, which is fragile and non-idempotent.</p>","path":["Containerization","Per-Service Users"],"tags":[]},{"location":"containerization/podman-over-docker/","level":1,"title":"Podman over Docker","text":"","path":["Containerization","Podman over Docker"],"tags":[]},{"location":"containerization/podman-over-docker/#what","level":2,"title":"What","text":"<p>I use Podman instead of Docker for all container workloads. Rootless. No daemon. No root.</p>","path":["Containerization","Podman over Docker"],"tags":[]},{"location":"containerization/podman-over-docker/#how","level":2,"title":"How","text":"<p>Podman runs every container as an unprivileged user process. There is no <code>dockerd</code> daemon running as root. Containers are managed by systemd through quadlet files, not through a long-running daemon process.</p> <p>Key configuration choices:</p> containers.conf<pre><code>[containers]\ndefault_rootless_network_cmd = \"pasta\"\n\n[engine]\nevents_logger = \"journald\"\ncgroup_manager = \"systemd\"\n</code></pre> storage.conf<pre><code>[storage]\ndriver = \"overlay\"\n\n[storage.options]\nnetwork_backend = \"netavark\"\n</code></pre> <p>The <code>unprivileged_userns_clone</code> sysctl is set to <code>0</code> — user namespaces are not cloned, maintaining strict UID isolation between host and container.</p>","path":["Containerization","Podman over Docker"],"tags":[]},{"location":"containerization/podman-over-docker/#why","level":2,"title":"Why","text":"<p>Docker's daemon runs as root. A container escape in Docker means full root access to the host. With Podman rootless, a container escape gives the attacker an unprivileged user namespace with no host access.</p> <p>Beyond security, Podman integrates natively with systemd. There is no separate orchestration layer needed — systemd manages the lifecycle, and quadlet files declare the container configuration. Docker requires Docker Compose or a separate init system to achieve the same thing.</p> <p>Docker also requires a persistent daemon. If <code>dockerd</code> crashes, all containers are orphaned. Podman containers are individual processes managed by systemd — if one fails, systemd restarts it independently.</p> <p>Why not Docker Desktop?</p> <p>Docker Desktop is irrelevant for server workloads. The comparison is between <code>dockerd</code> (Docker Engine) and Podman. On a headless server, Docker Desktop doesn't exist — the question is whether to run the Docker daemon or use Podman's daemonless model.</p>","path":["Containerization","Podman over Docker"],"tags":[]},{"location":"containerization/quadlets/","level":1,"title":"Quadlets","text":"","path":["Containerization","Quadlets"],"tags":[]},{"location":"containerization/quadlets/#what","level":2,"title":"What","text":"<p>I use Podman quadlets instead of Docker Compose files. Quadlets are declarative container definitions that integrate directly with systemd.</p>","path":["Containerization","Quadlets"],"tags":[]},{"location":"containerization/quadlets/#how","level":2,"title":"How","text":"<p>A quadlet file is a systemd unit file with container-specific sections. It lives in <code>~/.config/containers/systemd/</code> and systemd discovers it automatically — no <code>systemctl enable</code> needed.</p> myapp.container<pre><code>[Container]\nImage=docker.io/library/nginx:latest\nPublishPort=8080:80\nVolume=myapp-data.volume:/usr/share/nginx/html:ro\nNetwork=myapp.network\nDropCapability=ALL\nNoNewPrivileges=true\nReadOnly=true\nUserNS=keep-id\n\n[Service]\nRestart=always\nMemoryMax=512M\n\n[Install]\nWantedBy=default.target\n</code></pre> myapp.volume<pre><code>[Volume]\nVolumePath=/home/myapp/volumes/data\n\n[Install]\nWantedBy=default.target\n</code></pre> myapp.network<pre><code>[Network]\nNetworkName=myapp\nDriver=bridge\n\n[Install]\nWantedBy=default.target\n</code></pre> <p>Quadlet files generate corresponding systemd units:</p> Quadlet File Generated Unit <code>myapp.container</code> <code>myapp.service</code> <code>myapp.pod</code> <code>myapp.service</code> <code>myapp.volume</code> <code>myapp.service</code> <p>Cross-unit references require file extensions</p> <p>When referencing another quadlet unit, include the extension: <code>Pod=ai-services.pod</code> (correct), not <code>Pod=ai-services</code> (causes generator failure).</p>","path":["Containerization","Quadlets"],"tags":[]},{"location":"containerization/quadlets/#why","level":2,"title":"Why","text":"<p>Docker Compose is a separate orchestration system. It runs its own process, has its own state, and doesn't integrate with systemd. When a Compose-managed container crashes, Docker's restart policy handles it — but systemd doesn't know about it.</p> <p>Quadlets make systemd the single orchestrator. There is no separate <code>docker-compose up</code> process. systemd starts, stops, restarts, and monitors every container. <code>journalctl</code> captures container logs. <code>systemctl status</code> shows container state. Everything is in one place.</p> <p>The tradeoff is that quadlets are less widely documented than Docker Compose. But the integration with systemd is worth it — especially when managing 15+ services across multiple users.</p> <p>No <code>systemctl enable</code> needed</p> <p>The Podman systemd generator discovers quadlet files automatically. Drop a <code>.container</code> file in <code>~/.config/containers/systemd/</code> and systemd picks it up on the next daemon-reload.</p>","path":["Containerization","Quadlets"],"tags":[]},{"location":"containerization/the-ai-services-pod/","level":1,"title":"The AI Services Pod","text":"","path":["Containerization","The AI Services Pod"],"tags":[]},{"location":"containerization/the-ai-services-pod/#what","level":2,"title":"What","text":"<p>The AI service family is the one sanctioned exception to per-service-user isolation. All AI containers share a single user and a single Podman pod.</p>","path":["Containerization","The AI Services Pod"],"tags":[]},{"location":"containerization/the-ai-services-pod/#how","level":2,"title":"How","text":"<p>One user (<code>ai-services</code>), one pod (<code>ai-services.pod</code>), all AI containers joined to it:</p> ai-services.pod<pre><code>[Pod]\nPublishPort=\n\n[Install]\nWantedBy=default.target\n</code></pre> <p>Containers join the pod and communicate over localhost:</p> llamacpp.container<pre><code>[Container]\nPod=ai-services.pod\nImage=ghcr.io/ggml-org/llama.cpp:server-cuda12\nPublishPort=8090:8080\n</code></pre> <p>Service discovery uses localhost with different ports:</p> Service Internal Address Open WebUI <code>localhost:3000</code> llama.cpp (GPU) <code>localhost:8090</code> llama.cpp (CPU) <code>localhost:8091</code> SearXNG <code>localhost:8888</code> Docling-serve <code>localhost:5001</code> GitHub MCP <code>localhost:8082</code> Forgejo MCP <code>localhost:8085</code> <p>Containers declare systemd dependencies to avoid race conditions:</p> <pre><code>[Service]\nAfter=ai-services.pod llamacpp.service\nWants=ai-services.pod llamacpp.service\n</code></pre>","path":["Containerization","The AI Services Pod"],"tags":[]},{"location":"containerization/the-ai-services-pod/#why","level":2,"title":"Why","text":"<p>Before consolidation, each AI service had its own user and pod. This produced:</p> <ul> <li>Cross-container communication over the LAN instead of localhost</li> <li>Duplicate user management and lingering sessions</li> <li>A duplicated pod template across 5 roles — any pod change required updating 5 files</li> </ul> <p>The services share persistent data (llamacpp models directory, Open WebUI data) and are deployed as one logical unit. They already trust each other with shared volumes.</p> <p>The exception proves the rule</p> <p>This is a deliberate exception to per-service-user isolation. The container-level hardening (<code>DropCapability=ALL</code>, <code>NoNewPrivileges=true</code>, read-only rootfs, per-container <code>MemoryMax</code>) remains in place. The host UID is shared, but the primary isolation layer is user namespaces, not host UIDs.</p>","path":["Containerization","The AI Services Pod"],"tags":[]},{"location":"decisions/","level":1,"title":"Architecture Decision Records","text":"<p>ADRs document the reasoning behind major architectural choices. Each record captures the context, the decision, and the consequences — so the \"why\" is never lost.</p> ADR Decision Status ADR-0001 Rootless per-service <code>nologin</code> users for Podman Accepted ADR-0002 User-scoped systemd via <code>systemctl --machine=</code> Accepted ADR-0003 Shared <code>ai-services</code> pod for AI containers Accepted ADR-0004 Podman secrets for service credentials Accepted ADR-0005 Container timezone pass-through Accepted ADR-0006 oikb sources limited to docling-supported types Accepted ADR-0007 Docling sync timeout raised instead of PDF size cap Accepted ADR-0008 NVIDIA R580 via <code>.run</code> installer + APT pinning Accepted ADR-0010 Fixed context sizes replace <code>--fit</code> auto-fitting Accepted ADR-0011 Egress gate for rootless containers Accepted","path":["Decisions","Architecture Decision Records"],"tags":[]},{"location":"decisions/0001-rootless-per-service-users/","level":1,"title":"ADR-0001: Rootless per-service <code>nologin</code> users for Podman","text":"","path":["Decisions","ADR-0001: Rootless Users"],"tags":[]},{"location":"decisions/0001-rootless-per-service-users/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0001: Rootless Users"],"tags":[]},{"location":"decisions/0001-rootless-per-service-users/#context","level":2,"title":"Context","text":"<p>The home server runs 10+ containerized services (AI, media, automation, backup, monitoring). Containers are deployed as rootless Podman quadlets managed by systemd. Two viable alternatives to per-service users were evaluated:</p> <ul> <li>Rootful Podman with system-level quadlets in <code>/etc/containers/systemd/</code> — the Podman daemon runs as root, so a container escape means full host root. Volume ownership and network setup need root privileges.</li> <li>Shared service user (single <code>podman-services</code> UID for all containers) — a compromise of any container gives the attacker the same host UID for all other containers, no filesystem boundary, secrets visible across services.</li> </ul> <p>The per-service model was already in production; the question was whether the marginal security benefit justified the operational complexity.</p>","path":["Decisions","ADR-0001: Rootless Users"],"tags":[]},{"location":"decisions/0001-rootless-per-service-users/#decision","level":2,"title":"Decision","text":"<p>Keep rootless per-service users, each with a <code>nologin</code> shell (<code>system: no</code> normal users), one per service or per service family:</p> User Services <code>ai-services</code> llamacpp, open-webui, docling-serve, searxng, searxng-valkey, oikb, github-mcp, forgejo-mcp, openhands (shared pod) <code>healthchecks</code> healthchecks <code>jellyfin</code> jellyfin <code>homeassistant</code> homeassistant <code>forgejo</code> forgejo <code>rclone-user-a</code> / <code>rclone-user-b</code> rclone backups <p>The primary isolation comes from container-level controls (user namespaces <code>UserNS=keep-id</code>, <code>DropCapability=ALL</code>, <code>NoNewPrivileges=true</code>, <code>ReadOnly=true</code>, seccomp), which work regardless of host user model. The per-service user adds a separate host UID for file ownership, a separate Podman secret store, and a separate audit trail.</p> <p>Rejected options recorded for the record: rootful Podman (daemon-level trust boundary, accepted tradeoff documented in the analysis), shared service user (weakest isolation, not offset by operational gain).</p>","path":["Decisions","ADR-0001: Rootless Users"],"tags":[]},{"location":"decisions/0001-rootless-per-service-users/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: Container escape blast radius limited to a single UID; secrets isolated per user; containers stay unprivileged.</li> <li>Negative: User-scoped systemd units cannot be managed with the native <code>ansible.builtin.systemd</code> module (root cannot connect to another user's D-Bus socket) — see ADR-0002. User creation, <code>loginctl enable-linger</code>, and per-user systemd directories add operational overhead.</li> </ul>","path":["Decisions","ADR-0001: Rootless Users"],"tags":[]},{"location":"decisions/0001-rootless-per-service-users/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0002 (<code>systemctl --machine=</code> management)</li> <li>ADR-0003 (shared <code>ai-services</code> pod)</li> </ul>","path":["Decisions","ADR-0001: Rootless Users"],"tags":[]},{"location":"decisions/0002-user-scoped-systemd-machine/","level":1,"title":"ADR-0002: User-scoped systemd via <code>systemctl --machine=</code>","text":"","path":["Decisions","ADR-0002: --machine= Management"],"tags":[]},{"location":"decisions/0002-user-scoped-systemd-machine/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0002: --machine= Management"],"tags":[]},{"location":"decisions/0002-user-scoped-systemd-machine/#context","level":2,"title":"Context","text":"<p>Per-service <code>nologin</code> users (ADR-0001) mean container quadlets are user-scoped systemd units in <code>~/.config/containers/systemd/</code>. Ansible needs to <code>daemon-reload</code>, start, restart, and stop these units. Every \"native\" approach fails:</p> <ul> <li><code>ansible.builtin.systemd</code> with <code>scope: user</code> — root cannot connect to another user's D-Bus socket regardless of <code>XDG_RUNTIME_DIR</code>. This is a kernel-level socket permission block (<code>Operation not permitted</code>).</li> <li><code>sudoers env_keep</code> — same kernel-level restriction, not a sudo configuration issue.</li> <li><code>become_method: machinectl</code> — requires a valid login shell; <code>nologin</code> users fail.</li> <li><code>sudo -u &lt;nologin_user&gt; systemctl --user</code> — sudo does not re-run PAM, so the D-Bus session bus address is never set.</li> </ul> <p>The <code>systemctl --machine=&lt;user&gt;@.host --user</code> pattern works because it routes through the system bus (PID 1) via D-Bus, which root has access to, bypassing the per-user socket entirely. The <code>community.podman</code> collection has no quadlet/systemd integration.</p>","path":["Decisions","ADR-0002: --machine= Management"],"tags":[]},{"location":"decisions/0002-user-scoped-systemd-machine/#decision","level":2,"title":"Decision","text":"<p>Manage all user-scoped quadlet units through <code>systemctl --machine={{ user }}@.host --user</code> in Ansible <code>command</code> tasks and handlers. The pattern is centralized in <code>roles/podman-container-base/</code>:</p> <ul> <li>The role owns user creation, lingering, and per-user systemd directory setup.</li> <li>Every container role deploys its <code>.container</code> quadlet and runs an explicit <code>daemon-reload</code> + <code>start</code> on first deploy (bootstrap), with restart handlers wired through <code>--machine=</code>.</li> <li>Role-first, not module. A custom <code>podman_quadlet</code>/<code>systemd_user_unit</code> Ansible module is deferred until the true API is proven by repeated real-world usage across the role set. If a generic user-scoped-systemd pattern emerges, promote it to a module then.</li> </ul>","path":["Decisions","ADR-0002: --machine= Management"],"tags":[]},{"location":"decisions/0002-user-scoped-systemd-machine/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: One consistent, working pattern for all rootless quadlet roles; no new dependencies.</li> <li>Negative: Uses raw <code>command</code>/<code>shell</code> — no native <code>systemd</code> module semantics (<code>state</code>, <code>enabled</code>, change detection); first-deploy bootstrap requires explicit tasks, not just handlers.</li> <li>Revisit: If operational cost outweighs the security benefit of per-service users (ADR-0001), re-evaluate rootful Podman with system-level quadlets, which restores native <code>systemd</code> module use.</li> </ul>","path":["Decisions","ADR-0002: --machine= Management"],"tags":[]},{"location":"decisions/0002-user-scoped-systemd-machine/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0001, ADR-0003</li> </ul>","path":["Decisions","ADR-0002: --machine= Management"],"tags":[]},{"location":"decisions/0003-ai-services-pod/","level":1,"title":"ADR-0003: Shared <code>ai-services</code> pod for AI containers","text":"","path":["Decisions","ADR-0003: AI Services Pod"],"tags":[]},{"location":"decisions/0003-ai-services-pod/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0003: AI Services Pod"],"tags":[]},{"location":"decisions/0003-ai-services-pod/#context","level":2,"title":"Context","text":"<p>The AI service family (llamacpp, open-webui, docling-serve, searxng, searxng-valkey, oikb, github-mcp, forgejo-mcp, openhands) grew into a set of standalone containers. Each had its own user and pod, producing:</p> <ul> <li>Cross-container communication over the LAN/IP instead of localhost (open-webui calling llamacpp, docling-serve, searxng, MCP servers).</li> <li>Duplicate user management and lingering sessions per service.</li> <li>A duplicated pod template across 5 roles — any pod change had to be updated in 5 places.</li> </ul> <p>The services share persistent data (llamacpp models dir, open-webui data) and are deployed as one logical unit. The tradeoff of a shared user (single UID across services) was accepted here because container-level hardening — not the host UID — is the primary isolation layer (ADR-0001), and these services already trust each other with shared volumes.</p>","path":["Decisions","ADR-0003: AI Services Pod"],"tags":[]},{"location":"decisions/0003-ai-services-pod/#decision","level":2,"title":"Decision","text":"<p>Consolidate all AI containers into a single <code>ai-services</code> user and one shared <code>ai-services</code> pod:</p> <ul> <li>One user <code>ai-services</code> (nologin, per ADR-0001), one lingering session.</li> <li>One <code>ai-services.pod</code> quadlet pod template, used by all AI container roles.</li> <li>Containers join the pod via <code>Pod=ai-services.pod</code> and reach each other over localhost.</li> <li>Pod containers declare <code>After=</code>/<code>Wants=</code> on the pod unit (and on any containers they depend on at startup) to avoid race conditions — systemd starts all pod containers simultaneously without this.</li> </ul>","path":["Decisions","ADR-0003: AI Services Pod"],"tags":[]},{"location":"decisions/0003-ai-services-pod/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: One user + one pod = simpler user management; localhost URLs instead of network hops; single pod template to maintain.</li> <li>Negative: All AI containers share one host UID — a compromise of any single container exposes that UID to the rest (mitigated by <code>DropCapability=ALL</code>, <code>NoNewPrivileges</code>, read-only rootfs, per-container <code>MemoryMax</code>).</li> <li>Operational rule: dependency ordering inside the pod is explicit systemd <code>After=</code>/<code>Wants=</code>, never <code>sleep</code> hacks.</li> </ul>","path":["Decisions","ADR-0003: AI Services Pod"],"tags":[]},{"location":"decisions/0003-ai-services-pod/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0001 (per-service users — this is the sanctioned shared-user exception)</li> <li>ADR-0002</li> </ul>","path":["Decisions","ADR-0003: AI Services Pod"],"tags":[]},{"location":"decisions/0004-podman-secrets/","level":1,"title":"ADR-0004: Podman secrets for service credentials","text":"","path":["Decisions","ADR-0004: Podman Secrets"],"tags":[]},{"location":"decisions/0004-podman-secrets/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0004: Podman Secrets"],"tags":[]},{"location":"decisions/0004-podman-secrets/#context","level":2,"title":"Context","text":"<p>Services require credentials: OIDC client secrets, healthchecks <code>secret_key</code>, Authelia admin/session/storage/JWT secrets, API tokens (GitHub, Forgejo). Historically these were generated with <code>lookup()</code> and written to <code>.env</code> files on disk, or inlined into templates. That left plaintext credentials at rest outside any secret store, with no rotation story and a risk of accidental commit.</p>","path":["Decisions","ADR-0004: Podman Secrets"],"tags":[]},{"location":"decisions/0004-podman-secrets/#decision","level":2,"title":"Decision","text":"<p>Store all service credentials as Podman secrets and inject them via quadlet <code>Secret=</code> directives or at container start:</p> <ul> <li>Secret generation is idempotent: <code>podman secret inspect &lt;name&gt;</code> → create on miss with <code>openssl rand</code>.</li> <li>Secrets are generated at deploy time on the target host, never committed to the repo.</li> <li>Secret-material tasks run with <code>no_log: true</code>; where a <code>.env</code> file is still required, mode <code>0600</code>.</li> <li>Secrets are scoped to the service user's Podman store (ADR-0001) — per-user isolation preserved.</li> <li>Sensitive values that need human-readable storage (e.g., generated admin passwords) are echoed into the secret with stdout suppressed.</li> </ul>","path":["Decisions","ADR-0004: Podman Secrets"],"tags":[]},{"location":"decisions/0004-podman-secrets/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: No plaintext secrets on disk or in git; per-service isolation; rotation = recreate the secret + restart.</li> <li>Negative: Every secret adds a deploy-time generation task; <code>podman secret</code> lifecycle must be understood (secrets are deleted with the container unless the store is shared); not all images support secret injection — some still need env files.</li> <li>Migration debt: remaining <code>lookup()</code>/<code>.env</code> usages in roles should be audited and moved to Podman secrets.</li> </ul>","path":["Decisions","ADR-0004: Podman Secrets"],"tags":[]},{"location":"decisions/0004-podman-secrets/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0001 (per-user secret stores)</li> </ul>","path":["Decisions","ADR-0004: Podman Secrets"],"tags":[]},{"location":"decisions/0005-timezone-pass-through/","level":1,"title":"ADR-0005: Container timezone pass-through","text":"","path":["Decisions","ADR-0005: Timezone Pass-Through"],"tags":[]},{"location":"decisions/0005-timezone-pass-through/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0005: Timezone Pass-Through"],"tags":[]},{"location":"decisions/0005-timezone-pass-through/#context","level":2,"title":"Context","text":"<p>Hosts run <code>Europe/Your-Timezone</code> while containers defaulted to UTC, producing wrong timestamps in logs, UIs, and DB writes. Two mechanisms exist to pass the host timezone into a container:</p> <ul> <li>Mounting <code>/etc/localtime</code> — read by glibc/Go images (open-webui, oikb, docling-serve, llamacpp, openhands, jellyfin, github-mcp, forgejo-mcp).</li> <li><code>Environment=TZ=...</code> — required by Alpine/musl images (searxng, searxng-valkey, forgejo, homeassistant, healthchecks), which ignore <code>/etc/localtime</code>.</li> </ul> <p>One open question was whether auth services should be excluded. Research concluded: JWT <code>iat</code>/<code>exp</code>/<code>nbf</code> are epoch-second based and timezone-invariant — \"token issued in the future\" errors come from NTP clock drift, never from a timezone mount. So OAuth/SAML clients (open-webui) are safe to include; only token issuers are excluded.</p>","path":["Decisions","ADR-0005: Timezone Pass-Through"],"tags":[]},{"location":"decisions/0005-timezone-pass-through/#decision","level":2,"title":"Decision","text":"<p>Every non-auth container quadlet gets both:</p> <pre><code>Volume=/etc/localtime:/etc/localtime:ro\nEnvironment=TZ=Europe/Your-Timezone\n</code></pre> <p>The timezone is defined once in <code>playbooks/group_vars/home_server.yml</code>. The repo skeleton enforces this via a <code>rolename_tz_mount</code> switch (default <code>true</code>) in the container skeleton template — set <code>false</code> only for auth token issuers.</p>","path":["Decisions","ADR-0005: Timezone Pass-Through"],"tags":[]},{"location":"decisions/0005-timezone-pass-through/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: Correct timestamps in both glibc and musl images with one variable; enforced by the skeleton so new roles get it for free.</li> <li>Negative: One more mount + env per quadlet; the exclusion rule (token issuers only) must be respected — applying it to a token issuer would risk clock-skew confusion in OIDC debugging.</li> </ul>","path":["Decisions","ADR-0005: Timezone Pass-Through"],"tags":[]},{"location":"decisions/0006-oikb-docling-filter/","level":1,"title":"ADR-0006: oikb sources limited to docling-supported types","text":"","path":["Decisions","ADR-0006: Docling Filter"],"tags":[]},{"location":"decisions/0006-oikb-docling-filter/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0006: Docling Filter"],"tags":[]},{"location":"decisions/0006-oikb-docling-filter/#context","level":2,"title":"Context","text":"<p>oikb syncs external sources (GitHub repos, local directories) into Open WebUI Knowledge Bases, converting documents with the deployed docling-serve instance. The docling-serve image <code>quay.io/docling-project/docling-serve-cpu</code> ships docling 2.117.0 with no <code>soffice</code> (LibreOffice) and no <code>ffmpeg</code> — so legacy Office formats (<code>.doc</code>/<code>.xls</code>/<code>.ppt</code>) and audio/video are not convertible. Unfiltered sync sources produced conversion failures on every sync cycle.</p>","path":["Decisions","ADR-0006: Docling Filter"],"tags":[]},{"location":"decisions/0006-oikb-docling-filter/#decision","level":2,"title":"Decision","text":"<p>Restrict every oikb sync source to the <code>docling_filter</code> allowlist defined once in <code>playbooks/group_vars/home_server.yml</code>:</p> <ul> <li>The allowlist is the intersection of what the deployed docling-serve image can convert and Open WebUI's accepted KB upload types.</li> <li>Excluded types: <code>.doc</code>/<code>.xls</code>/<code>.ppt</code> (needs LibreOffice), audio/video (needs ffmpeg + ASR extra).</li> <li>The filter is shared across all sources via a YAML anchor, so a new source inherits the restriction automatically.</li> </ul>","path":["Decisions","ADR-0006: Docling Filter"],"tags":[]},{"location":"decisions/0006-oikb-docling-filter/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: No sync failures from unsupported formats; one source of truth for the allowlist; adding a format is a single edit.</li> <li>Negative: Office/AV content in source repos is silently skipped — operators must know the intersection rule; changing the docling-serve image (e.g., the full <code>docling-serve</code> variant with soffice) would require re-reviewing the allowlist.</li> </ul>","path":["Decisions","ADR-0006: Docling Filter"],"tags":[]},{"location":"decisions/0006-oikb-docling-filter/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0007 (timeout policy for large documents)</li> </ul>","path":["Decisions","ADR-0006: Docling Filter"],"tags":[]},{"location":"decisions/0007-docling-sync-timeout/","level":1,"title":"ADR-0007: Docling sync timeout raised instead of PDF size cap","text":"","path":["Decisions","ADR-0007: Docling Timeout"],"tags":[]},{"location":"decisions/0007-docling-sync-timeout/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0007: Docling Timeout"],"tags":[]},{"location":"decisions/0007-docling-sync-timeout/#context","level":2,"title":"Context","text":"<p>Large PDFs (13 MB+) exceeded the docling-serve synchronous conversion default <code>DOCLING_SERVE_MAX_SYNC_WAIT=120</code> seconds. The sync endpoint returned 504, and Open WebUI re-submitted the document on every sync cycle, pinning docling-serve at full CPU. The alternative considered was capping PDF size in the oikb filter (ADR-0006) — rejected because it silently drops legitimate documents.</p>","path":["Decisions","ADR-0007: Docling Timeout"],"tags":[]},{"location":"decisions/0007-docling-sync-timeout/#decision","level":2,"title":"Decision","text":"<p>Support large documents by raising sync timeouts, not by capping size:</p> <ul> <li><code>DOCLING_SERVE_MAX_SYNC_WAIT: 600</code> — official docs recommend 600s for large documents.</li> <li><code>docling_serve_eng_loc_num_workers: 4</code> plus thread knobs (<code>DOCLING_NUM_THREADS</code> / <code>OMP_NUM_THREADS</code> / <code>MKL_NUM_THREADS=4</code>) for conversion throughput.</li> <li><code>UVICORN_WORKERS</code> stays at 1 — multiple workers cause \"Task Not Found\" errors with async conversion.</li> </ul>","path":["Decisions","ADR-0007: Docling Timeout"],"tags":[]},{"location":"decisions/0007-docling-sync-timeout/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: Large PDFs convert successfully; no documents silently dropped; no re-submit storm.</li> <li>Negative: A single conversion can hold a sync request for up to 600s; resource tuning (workers/threads) is a balance — workers ≠ single-doc latency due to the GIL, hence the thread knobs.</li> </ul>","path":["Decisions","ADR-0007: Docling Timeout"],"tags":[]},{"location":"decisions/0007-docling-sync-timeout/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0006 (the filter allowlist is unchanged — no size cap added)</li> </ul>","path":["Decisions","ADR-0007: Docling Timeout"],"tags":[]},{"location":"decisions/0008-nvidia-r580-run-installer/","level":1,"title":"ADR-0008: NVIDIA R580 via <code>.run</code> installer + APT pinning","text":"","path":["Decisions","ADR-0008: NVIDIA R580"],"tags":[]},{"location":"decisions/0008-nvidia-r580-run-installer/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0008: NVIDIA R580"],"tags":[]},{"location":"decisions/0008-nvidia-r580-run-installer/#context","level":2,"title":"Context","text":"<p>The GPU is a Tesla P40 (Pascal, GP102). Three constraints collide:</p> Constraint Impact NVIDIA dropped Pascal support from R590+ Cannot use R590 or newer Containers use <code>server-cuda12</code> images built against CUDA 12.8 Requires driver ≥ R570 Debian 13 <code>non-free</code> ships only R550 (CUDA 12.4) Too old for CUDA 12.8 containers <p>The intended solution was NVIDIA's official <code>debian13</code> CUDA repo pinned to R580. It failed in practice: NVIDIA's <code>debian13</code> repo does not contain R580 packages (only R590+). The only way to obtain R580 on Debian 13 is NVIDIA's <code>.run</code> installer (<code>NVIDIA-Linux-x86_64-580.173.02.run</code>).</p>","path":["Decisions","ADR-0008: NVIDIA R580"],"tags":[]},{"location":"decisions/0008-nvidia-r580-run-installer/#decision","level":2,"title":"Decision","text":"<p>Install the R580 driver from the NVIDIA <code>.run</code> installer, not from APT:</p> <ul> <li>The role downloads and runs <code>NVIDIA-Linux-x86_64-580.173.02.run</code> with DKMS (kernel headers metapackage for future upgrades).</li> <li>The old cobbled CUDA repo (Debian 12 variant, SHA1 key) is purged; Debian 13's Sequoia-PGP rejects SHA1 since 2026-02-01.</li> <li><code>nvidia-container-toolkit</code> from its official repo; CDI generated via <code>nvidia-ctk config</code> (<code>mode=cdi</code>, <code>no-cgroups</code>) for rootless Podman GPU passthrough.</li> <li>APT preferences pin any remaining <code>nvidia</code>/<code>cuda</code> packages to version <code>580*</code> (Pin-Priority 1001, downgrade allowed) so tooling never pulls an unsupported branch.</li> <li>A manual cleanup + reboot phase runs on the host before the playbook (a loaded driver cannot be purged mid-session).</li> </ul>","path":["Decisions","ADR-0008: NVIDIA R580"],"tags":[]},{"location":"decisions/0008-nvidia-r580-run-installer/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: P40 gets CUDA 12.8-compatible R580 acceleration; CDI exposes <code>nvidia.com/gpu=all</code> to rootless containers; llama.cpp runs on GPU.</li> <li>Negative: The <code>.run</code> installer is not apt-managed (updates are manual); requires an out-of-band host prep step; upgrading the GPU later requires revisiting this ADR.</li> </ul>","path":["Decisions","ADR-0008: NVIDIA R580"],"tags":[]},{"location":"decisions/0008-nvidia-r580-run-installer/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0001 (rootless container GPU passthrough)</li> </ul>","path":["Decisions","ADR-0008: NVIDIA R580"],"tags":[]},{"location":"decisions/0010-fixed-context-sizes-replace-fit/","level":1,"title":"ADR-0010: Fixed context sizes replace <code>--fit</code> auto-fitting for llamacpp presets","text":"","path":["Decisions","ADR-0010: Fixed Context Sizes"],"tags":[]},{"location":"decisions/0010-fixed-context-sizes-replace-fit/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0010: Fixed Context Sizes"],"tags":[]},{"location":"decisions/0010-fixed-context-sizes-replace-fit/#context","level":2,"title":"Context","text":"<p>llamacpp presets were tuned with llama.cpp's <code>--fit</code> memory automation (<code>fit = on</code>, <code>fit-ctx</code>, <code>fit-target</code>). Two problems emerged:</p> <ol> <li> <p>OpenHands cannot see the context size at all — with or without fit.    OpenHands Agent Canvas determines an LLM's context window from exactly two sources: an explicit <code>max_input_tokens</code> field on its LLM config, or litellm's static model registry keyed by name. It never queries llama.cpp's <code>/props</code> or <code>/v1/models</code>. For aliases like \"Qwen 3.6 - Coding\" every registry lookup fails, so <code>effective_max_input_tokens</code> stays unset. The proactive, token-pressure condensation path is therefore dead: compaction only fires via the 240-event heuristic or the reactive retry when the LLM raises a context-window error — which llama.cpp does not produce by default (it truncates/context-shifts instead).</p> </li> <li> <p>Fit-produced context sizes are load-time surprises, not contracts. <code>fit-ctx</code> is a minimum floor for the fitter, not a set point. Worse, pinning any memory argument (e.g. <code>ngl = 99</code>) makes the whole fit pass abort — observed on the server as <code>common_fit_params: failed to fit params to free device memory: n_gpu_layers already set by user to 99, abort</code>, after which the context silently fell back to the configured floor.</p> </li> </ol> <p>llama.cpp reports the real context only after a model loads; unloaded presets expose none. A deterministic, explicit value per preset is the only thing downstream consumers can rely on.</p>","path":["Decisions","ADR-0010: Fixed Context Sizes"],"tags":[]},{"location":"decisions/0010-fixed-context-sizes-replace-fit/#decision","level":2,"title":"Decision","text":"<p>Replace fit-derived context sizing with explicit per-model context contracts in <code>playbooks/group_vars/home_server.yml</code> (<code>params.ctx_size</code>), rendered by the llamacpp template as <code>ctx-size</code> replacing the <code>fit-ctx</code>/<code>fit-target</code> blocks. All fit keys are removed from the <code>[*]</code> global section and from model params.</p> Preset ctx-size Notes Gemma 4 - Text,Image 262144 uniform contract; ngl pin removed Qwen 3.6 - Coding 262144 fit places layers around fixed <code>-c</code> Qwen 3.8 - Text,Image 262144 native context window; fallback ladder if OOM: 196608 → 131072 <p>Rationale per llama.cpp semantics: an explicit <code>--ctx-size</code> is never changed by <code>--fit</code>, so the fitter keeps managing GPU/RAM tensor placement around a known context.</p> <p>OpenHands pairing step (manual): set the Qwen Coding LLM's max input tokens to ~245760 (262144 minus 16k output headroom). Headroom absorbs litellm-vs-llama.cpp tokenizer drift and reserves output room near the ceiling.</p>","path":["Decisions","ADR-0010: Fixed Context Sizes"],"tags":[]},{"location":"decisions/0010-fixed-context-sizes-replace-fit/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: Deterministic context budgets; OpenHands token-pressure condensation works once <code>max_input_tokens</code> is set; no silent fallback to the 8192 floor; reported <code>meta.n_ctx</code> matches config verbatim; uniform 262144 contracts across all production presets mean a single OpenHands <code>max_input_tokens</code> value (~245760) is valid regardless of loaded model.</li> <li>Negative: KV cache is always allocated at full contract size — oversized values either fail loudly at load (restart loop, when memory args are pinned) or degrade silently as fit sheds expert tensors to CPU (unpinned); VRAM can no longer flex down for co-tenant GPU workloads via fit-target.</li> <li>Measured cost of uniform 256k on P40 (Gemma @ 262144 vs 131072, accepted 2026-08): token generation ~35 → ~31 t/s (−11%), prompt processing ~446 → ~81 t/s (−82%, PCIe bottleneck from CPU-resident experts). Fallback ladder if revisited: ctx 245760 → 196608 → 131072.</li> </ul>","path":["Decisions","ADR-0010: Fixed Context Sizes"],"tags":[]},{"location":"decisions/0010-fixed-context-sizes-replace-fit/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0003 (ai-services pod)</li> </ul>","path":["Decisions","ADR-0010: Fixed Context Sizes"],"tags":[]},{"location":"decisions/0011-egress-gate-for-rootless-containers/","level":1,"title":"ADR-0011: Egress gate for rootless containers","text":"","path":["Decisions","ADR-0011: Egress Gate"],"tags":[]},{"location":"decisions/0011-egress-gate-for-rootless-containers/#status","level":2,"title":"Status","text":"<p>Accepted</p>","path":["Decisions","ADR-0011: Egress Gate"],"tags":[]},{"location":"decisions/0011-egress-gate-for-rootless-containers/#context","level":2,"title":"Context","text":"<p>Rootless Podman's default network mode on this server is pasta. At unit start pasta snapshots the host's addressing, routes and DNS configuration (<code>--config-net</code>) and never refreshes it while running.</p> <p>Incident 2026-08-24: boot at 15:02:33, containers started 15:02:50–52 (~17 s after boot). At that moment LAN/DNS had not settled, so every pasta instance snapshotted an unsettled network. Consequences for the rest of the day:</p> <ul> <li>Host egress worked; all container egress failed instantly (raw-IP connect refused, DNS dead).</li> <li>OpenHands Agent Canvas could not reach its hosted MCP endpoint — \"MCP tool listing timed out after 30 seconds\" and \"Could not reach the server\".</li> <li>HomeAssistant lost weather data — same root cause.</li> </ul> <p>Restarting the affected user services forced fresh snapshots and fixed everything immediately. The repo history confirmed no networking misconfiguration — this is a pure boot-order race, and it recurs on any reboot where networking takes longer than ~15–20 s to settle.</p>","path":["Decisions","ADR-0011: Egress Gate"],"tags":[]},{"location":"decisions/0011-egress-gate-for-rootless-containers/#decision","level":2,"title":"Decision","text":"<p>Gate all rootless container units behind an egress wait, implemented centrally in <code>podman-container-base</code> (every container role already imports it):</p> <ul> <li>New script <code>~/.local/bin/wait-for-egress</code> (per service user): bounded loop (default 120 s, 3 s interval) that exits 0 only when the host has a default route, a successful TCP probe (<code>1.1.1.1:443</code>), and working DNS (<code>getent hosts github.com</code>).</li> <li>systemd drop-in <code>&lt;unit&gt;.service.d/egress-gate.conf</code> per managed unit (and per managed pod):<ul> <li><code>ExecStartPre=/home/&lt;user&gt;/.local/bin/wait-for-egress</code></li> <li><code>TimeoutStartSec=&lt;timeout + 30&gt;s</code></li> <li><code>RestartSec=15s</code> (prevents a 100 ms-default restart hammer-loop while the network is down)</li> </ul> </li> <li>Default on for every role importing the base, including offline-capable services. Opt-out per role via <code>pcb_egress_gate: false</code>.</li> <li>Drop-ins live outside the quadlet templates, so quadlet files stay untouched and future roles get the gate automatically.</li> </ul>","path":["Decisions","ADR-0011: Egress Gate"],"tags":[]},{"location":"decisions/0011-egress-gate-for-rootless-containers/#consequences","level":2,"title":"Consequences","text":"<ul> <li>Positive: Boots self-heal — containers simply start later, healthy; no stale-snapshot state possible; single implementation point covers all current and future roles.</li> <li>Negative: Offline-capable services (llamacpp serving local clients) also wait for internet at boot — accepted for simplicity over per-role bookkeeping. Playbook runs block up to ~2 min per gated start if the network is down mid-run. Crash-loop recovery slows to ≥ 15 s per attempt (<code>RestartSec</code>).</li> <li>Operational rule: Never remove or bypass the gate without solving the underlying snapshot race; new container roles get the gate for free via <code>podman-container-base</code>.</li> </ul>","path":["Decisions","ADR-0011: Egress Gate"],"tags":[]},{"location":"decisions/0011-egress-gate-for-rootless-containers/#related","level":2,"title":"Related","text":"<ul> <li>ADR-0001 (rootless users)</li> <li>ADR-0002 (<code>--machine=</code> user-scoped systemd)</li> <li>ADR-0003 (shared pod)</li> </ul>","path":["Decisions","ADR-0011: Egress Gate"],"tags":[]},{"location":"infrastructure-as-code/","level":1,"title":"Infrastructure as Code","text":"<p>The homelab is managed by Ansible. Every role, every configuration, every deployment is defined in code.</p> <ul> <li>Ansible Patterns — roles, tags, group_vars, feature flags</li> <li>Secrets Management — Podman secrets over .env files</li> <li>Pre-commit &amp; Linting — ansible-lint, yamllint, detect-secrets</li> </ul>","path":["Infrastructure as Code"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/","level":1,"title":"Ansible Patterns","text":"","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#what","level":2,"title":"What","text":"<p>The Ansible repo follows consistent patterns across all 41 roles: shared base role, tag-based organization, feature flags, and group_vars for overrides.</p>","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#how","level":2,"title":"How","text":"","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#role-structure","level":3,"title":"Role Structure","text":"<p>Every container role follows the same pattern:</p> <ol> <li>Import <code>podman-container-base</code> (user setup, lingering, egress gate, image prune)</li> <li>Create ZFS dataset via <code>include_role: zfs-dataset</code></li> <li>Template quadlet files (<code>.container</code>, <code>.pod</code>, <code>.network</code>, <code>.volume</code>)</li> <li>Notify handler for restart</li> </ol> <p>The <code>podman-container-base</code> role is the shared lifecycle manager. It handles:</p> <ul> <li>Service user creation with <code>nologin</code> shell</li> <li><code>loginctl enable-linger</code> for systemd user sessions</li> <li>Egress gate deployment (<code>wait-for-egress</code> + systemd drop-in)</li> <li>Weekly image pruning (<code>podman image prune -a -f</code>)</li> </ul>","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#tags","level":3,"title":"Tags","text":"<p>Playbooks use tags for selective execution:</p> Tag Scope <code>bootstrap</code> User creation, system setup <code>system</code> Kernel parameters, sysctl <code>networking</code> Firewall, VPN <code>container</code> Quadlet deployment <code>monitoring</code> Healthchecks <code>backup</code> Sanoid, syncoid, rclone","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#feature-flags","level":3,"title":"Feature Flags","text":"<p>Services are toggled via <code>&lt;service&gt;.enabled | default(false)</code>:</p> <pre><code># group_vars/home_server.yml\njellyfin_enabled: false\nhomeassistant_enabled: false\n</code></pre>","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#group-vars","level":3,"title":"Group Vars","text":"<p>Configuration is layered:</p> File Scope <code>playbooks/group_vars/all.yml</code> All hosts <code>playbooks/group_vars/home_server.yml</code> Home server only <code>playbooks/group_vars/disaster_recovery.yml</code> DR host only <code>playbooks/group_vars/proxy.yml</code> Proxy host only <p>Role defaults in <code>roles/&lt;role&gt;/defaults/main.yml</code> provide the base values. Group_vars override per host.</p>","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/ansible-patterns/#why","level":2,"title":"Why","text":"<p>Consistency is the point. When every role follows the same pattern, adding a new service is mechanical:</p> <ol> <li>Copy an existing role</li> <li>Change the quadlet template</li> <li>Add the ZFS dataset</li> <li>Add the feature flag</li> </ol> <p>The pattern is proven — it works for 41 roles. New roles don't need to reinvent the lifecycle.</p>","path":["Infrastructure as Code","Ansible Patterns"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/","level":1,"title":"Pre-commit &amp; Linting","text":"","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/#what","level":2,"title":"What","text":"<p>Pre-commit hooks enforce code quality before every commit. Linting catches errors before they reach the server.</p>","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/#how","level":2,"title":"How","text":"","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/#pre-commit-hooks","level":3,"title":"Pre-commit Hooks","text":".pre-commit-config.yaml<pre><code>repos:\n  - repo: https://github.com/ansible/ansible-lint\n    hooks:\n      - id: ansible-lint\n  - repo: https://github.com/adrienverge/yamllint\n    hooks:\n      - id: yamllint\n  - repo: https://github.com/ibmdb/detect-secrets\n    hooks:\n      - id: detect-secrets\n        args: ['--baseline', '.secrets.baseline']\n</code></pre>","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/#template-linting","level":3,"title":"Template Linting","text":"<p>A custom script validates Jinja2 templates:</p> scripts/lint-templates.py<pre><code># Renders every .j2 template, then runs bash -n on the output\n# Catches syntax errors before deployment\n</code></pre>","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/#branch-protection","level":3,"title":"Branch Protection","text":"<p>The <code>no-commit-to-branch</code> hook prevents direct commits to <code>main</code>:</p> <pre><code>  - repo: https://github.com/pre-commit/pre-commit-hooks\n    hooks:\n      - id: no-commit-to-branch\n        args: ['--branch', 'main']\n</code></pre>","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/pre-commit-linting/#why","level":2,"title":"Why","text":"<p>The lint pipeline catches three categories of errors:</p> <ol> <li>YAML syntax — malformed files that Ansible would reject at runtime</li> <li>Ansible best practices — deprecated modules, missing become, insecure permissions</li> <li>Secret leaks — API keys, tokens, or passwords accidentally committed</li> </ol> <p>The template linter is custom because standard YAML linting doesn't catch Jinja2 render errors. A template that renders to invalid bash or invalid systemd is invisible to YAML lint — but the custom script renders every template and validates the output.</p> <p>Run before commit</p> <p>Pre-commit runs automatically, but you can also run manually:</p> <pre><code>pre-commit run --all-files\n</code></pre>","path":["Infrastructure as Code","Pre-commit & Linting"],"tags":[]},{"location":"infrastructure-as-code/secrets-management/","level":1,"title":"Secrets Management","text":"","path":["Infrastructure as Code","Secrets Management"],"tags":[]},{"location":"infrastructure-as-code/secrets-management/#what","level":2,"title":"What","text":"<p>Service credentials are stored as Podman secrets, not <code>.env</code> files. Secrets are generated at deploy time, never committed to the repo.</p>","path":["Infrastructure as Code","Secrets Management"],"tags":[]},{"location":"infrastructure-as-code/secrets-management/#how","level":2,"title":"How","text":"<p>Secret creation is idempotent:</p> <pre><code>- name: Check if secret exists\n  command: podman secret inspect myservice-secret\n  register: secret_check\n  failed_when: false\n  changed_when: false\n\n- name: Create secret\n  command: podman secret create myservice-secret -\n  stdin: \"{{ lookup('ansible.builtin.password', length=32) }}\"\n  when: secret_check.rc != 0\n  no_log: true\n</code></pre> <p>Secrets are injected via quadlet <code>Secret=</code> directives:</p> <pre><code>[Container]\nSecret=myservice-secret\n</code></pre> <p>Or via <code>EnvironmentFile</code> for images that don't support secret injection:</p> <pre><code>[Container]\nEnvironmentFile=%h/secrets.env\n</code></pre>","path":["Infrastructure as Code","Secrets Management"],"tags":[]},{"location":"infrastructure-as-code/secrets-management/#why","level":2,"title":"Why","text":"<p><code>.env</code> files leave plaintext credentials on disk. If the host is compromised, every <code>.env</code> file is readable. Podman secrets are stored in the user's Podman secret store — encrypted at rest, scoped to the user.</p> <p>The <code>no_log: true</code> directive prevents Ansible from logging secret values. Without it, the generated secret would appear in playbook output.</p> <p>Migration debt</p> <p>Not all roles have been migrated to Podman secrets. Some still use <code>.env</code> files with <code>lookup()</code> generation. These should be migrated — the pattern is proven and the migration is straightforward.</p>","path":["Infrastructure as Code","Secrets Management"],"tags":[]},{"location":"networking/","level":1,"title":"Networking","text":"<p>Networking in a homelab is about two things: connecting services securely and keeping everything else out.</p> <ul> <li>Mesh VPN — DNClient / managed Nebula</li> <li>Firewalls — UFW on Debian, PF on FreeBSD</li> <li>Reverse Proxy — Caddy on a VPS, auto-HTTPS</li> </ul>","path":["Networking"],"tags":[]},{"location":"networking/firewall-ufw-pf/","level":1,"title":"Firewalls","text":"","path":["Networking","Firewalls"],"tags":[]},{"location":"networking/firewall-ufw-pf/#what","level":2,"title":"What","text":"<p>Two firewalls, two operating systems, one philosophy: deny by default, allow only what's needed.</p>","path":["Networking","Firewalls"],"tags":[]},{"location":"networking/firewall-ufw-pf/#how","level":2,"title":"How","text":"","path":["Networking","Firewalls"],"tags":[]},{"location":"networking/firewall-ufw-pf/#debian-home-server-proxy-ufw","level":3,"title":"Debian (Home Server + Proxy) — UFW","text":"<pre><code>firewall_default: deny\nfirewall_incoming:\n  - { port: 22, proto: tcp }     # SSH\n  - { port: 8090, proto: tcp }   # llama.cpp GPU\n  - { port: 8091, proto: tcp }   # llama.cpp CPU\n  # ... additional service ports\nfirewall_outgoing: allow\n</code></pre>","path":["Networking","Firewalls"],"tags":[]},{"location":"networking/firewall-ufw-pf/#freebsd-dr-host-pf","level":3,"title":"FreeBSD (DR Host) — PF","text":"<p>PF rules are deployed via Jinja2 template and validated with <code>pfctl -nf</code> before activation:</p> <pre><code>block in all\npass out all\npass in on egress proto tcp to any port 22\n</code></pre> <p>Enabled in <code>rc.conf</code>:</p> <pre><code>pf_enable=\"YES\"\npf_rules=\"/etc/pf.conf\"\n</code></pre>","path":["Networking","Firewalls"],"tags":[]},{"location":"networking/firewall-ufw-pf/#why","level":2,"title":"Why","text":"<p>UFW is the default firewall tool on Debian — it's well-integrated, simple, and sufficient for a single-host firewall. PF is the standard on FreeBSD — more powerful, more expressive, and the expected tool on that platform.</p> <p>I don't force one tool across both OSes. Each firewall follows the convention of its platform. The philosophy is the same (deny default, allow explicit), but the implementation matches the OS.</p> <p>PF rule validation</p> <p>The PF template is validated with <code>pfctl -nf</code> before deployment. A syntax error in PF rules can lock you out of a remote host. The validation step catches errors before they reach the firewall.</p>","path":["Networking","Firewalls"],"tags":[]},{"location":"networking/mesh-vpn/","level":1,"title":"Mesh VPN","text":"","path":["Networking","Mesh VPN"],"tags":[]},{"location":"networking/mesh-vpn/#what","level":2,"title":"What","text":"<p>All hosts are connected via DNClient — a managed Nebula mesh VPN from Defined Networking. Every server, the DR host, and the reverse proxy join the same overlay network.</p>","path":["Networking","Mesh VPN"],"tags":[]},{"location":"networking/mesh-vpn/#how","level":2,"title":"How","text":"<p>DNClient is deployed on every host. The binary lives at <code>/usr/local/bin/dnclient</code> and is auto-installed from the Defined API. Each host gets a mesh address in the <code>10.0.0.x</code> range.</p> <p>Services bind to their mesh address, not the public internet. The reverse proxy routes traffic from public DNS to mesh addresses. NFS is restricted to the mesh subnet:</p> <pre><code>nfs_exports:\n  - path: /bulkdata/Media\n    clients: \"10.0.0.0/24\"\n    options: \"all_squash,anonuid=0,anongid=451\"\n</code></pre>","path":["Networking","Mesh VPN"],"tags":[]},{"location":"networking/mesh-vpn/#why","level":2,"title":"Why","text":"<p>Defined Networking is managed Nebula. The open-source Nebula project provides the overlay network — certificate-based identity, encrypted tunnels, and firewall policy. Defined adds a management plane on top: automatic cert rotation, NAT traversal (UDP hole punching + relays), and a central dashboard.</p> <p>The key architectural insight: the management plane (Defined API) is separate from the control plane. The overlay keeps running even if the Defined API is unreachable. DNS resolution, firewall rules, and tunnel establishment all happen locally after initial enrollment.</p> <p>Why not WireGuard?</p> <p>WireGuard is excellent for point-to-point tunnels. But a homelab with 4+ hosts needs mesh connectivity — every host must reach every other host. With WireGuard, that means N×(N-1)/2 tunnels, each manually managed. Nebula's mesh overlay handles this automatically: add a host, and it discovers and connects to every other host.</p>","path":["Networking","Mesh VPN"],"tags":[]},{"location":"networking/reverse-proxy/","level":1,"title":"Reverse Proxy","text":"","path":["Networking","Reverse Proxy"],"tags":[]},{"location":"networking/reverse-proxy/#what","level":2,"title":"What","text":"<p>A dedicated Debian 13 VPS runs Caddy as a reverse proxy. It terminates TLS and routes traffic to services on the home server via the mesh VPN.</p>","path":["Networking","Reverse Proxy"],"tags":[]},{"location":"networking/reverse-proxy/#how","level":2,"title":"How","text":"<p>Caddy handles automatic HTTPS via Let's Encrypt (ACME). Routes point to mesh addresses:</p> <pre><code>jellyfin.example.com {\n    reverse_proxy 10.0.0.4:8096\n}\n\nhomeassistant.example.com {\n    reverse_proxy 10.0.0.4:8123\n}\n\nai.example.com {\n    reverse_proxy 10.0.0.4:3000\n}\n\ngit.example.com {\n    reverse_proxy 10.0.0.4:3001\n}\n</code></pre> <p>The proxy host has its own Ansible playbook and bootstrap process: create an <code>ansible</code> user, disable root SSH, then run everything as <code>ansible</code>.</p>","path":["Networking","Reverse Proxy"],"tags":[]},{"location":"networking/reverse-proxy/#why","level":2,"title":"Why","text":"<p>The home server sits behind a NAT. Public-facing services need a reverse proxy with a public IP. The VPS provides that endpoint.</p> <p>Caddy was chosen over Nginx or Traefik because:</p> <ul> <li>Automatic HTTPS with zero configuration — Caddy handles certificate renewal natively</li> <li>Simple file-based config — no dynamic service discovery needed for 6 services</li> <li>Single binary, no daemon dependencies</li> </ul> <p>The mesh VPN means the proxy connects to services over encrypted tunnels, not the public internet. Traffic flow: public internet → Caddy (VPS) → Nebula tunnel → service (home server).</p> <p>Why not Cloudflare Tunnel?</p> <p>Cloudflare Tunnel would eliminate the need for a VPS entirely. But it routes traffic through Cloudflare's infrastructure, which adds latency and means trusting a third party with all request headers. The VPS + Caddy approach keeps traffic on infrastructure I control.</p>","path":["Networking","Reverse Proxy"],"tags":[]},{"location":"services/","level":1,"title":"Services","text":"<p>The supporting services that round out the homelab.</p> <ul> <li>Forgejo — self-hosted git forge</li> <li>Healthchecks — ping-based monitoring</li> <li>NFS — VPN-restricted file sharing</li> <li>Toggleable Services — Jellyfin, Home Assistant</li> </ul>","path":["Services"],"tags":[]},{"location":"services/forgejo/","level":1,"title":"Forgejo","text":"","path":["Services","Forgejo"],"tags":[]},{"location":"services/forgejo/#what","level":2,"title":"What","text":"<p>Forgejo is my self-hosted git forge. It hosts the Obsidian vault repository and any private code that doesn't belong on GitHub.</p>","path":["Services","Forgejo"],"tags":[]},{"location":"services/forgejo/#how","level":2,"title":"How","text":"<p>Runs as <code>codeberg.org/forgejo/forgejo:15-rootless</code> with SQLite — no Postgres dependency for a single-user instance.</p> <p>Accessible at <code>git.example.com</code> via the Caddy reverse proxy. SSH on port 2222 (non-standard to avoid conflicts with the host SSH).</p>","path":["Services","Forgejo"],"tags":[]},{"location":"services/forgejo/#why","level":2,"title":"Why","text":"<p>Forgejo over Gitea because:</p> <ul> <li>Forgejo is community-governed (Gitea Ltd has a history of license changes)</li> <li>Rootless container support is first-class</li> <li>Feature set is identical for my use case (single-user, SSH, web UI)</li> </ul> <p>SQLite over Postgres because:</p> <ul> <li>Single-user forge doesn't need Postgres concurrency</li> <li>One fewer container to manage</li> <li>Backup is a single file copy</li> </ul> <p>The Obsidian vault</p> <p>The Obsidian vault (<code>ObsidianVault-Personal</code>) is the primary repository hosted on Forgejo. oikb syncs it into Open WebUI's knowledge base for RAG.</p>","path":["Services","Forgejo"],"tags":[]},{"location":"services/healthchecks/","level":1,"title":"Healthchecks","text":"","path":["Services","Healthchecks"],"tags":[]},{"location":"services/healthchecks/#what","level":2,"title":"What","text":"<p>healthchecks.io runs as a lightweight ping-based monitoring service. Services send periodic pings; if a ping doesn't arrive, healthchecks alerts.</p>","path":["Services","Healthchecks"],"tags":[]},{"location":"services/healthchecks/#how","level":2,"title":"How","text":"<p>Runs as <code>docker.io/healthchecks/healthchecks:latest</code> with a Podman secret for <code>secret_key</code>.</p> <p>Accessible at <code>healthchecks.example.com</code> via the Caddy reverse proxy.</p>","path":["Services","Healthchecks"],"tags":[]},{"location":"services/healthchecks/#why","level":2,"title":"Why","text":"<p>I tried Prometheus + Grafana. I had 400 dashboards I never looked at and alert rules I never tuned. The signal-to-noise ratio was terrible.</p> <p>healthchecks.io does one thing: tells me if a service is down. A service sends a ping every N minutes. If the ping doesn't arrive, healthchecks sends a notification. That's it.</p> <p>The tradeoff is visibility — I can't see CPU usage, memory trends, or request latency. But for a homelab, the question that matters is \"is it working?\" not \"how efficiently is it working?\"</p>","path":["Services","Healthchecks"],"tags":[]},{"location":"services/nfs/","level":1,"title":"NFS","text":"","path":["Services","NFS"],"tags":[]},{"location":"services/nfs/#what","level":2,"title":"What","text":"<p>NFS exports media storage from the home server to VPN clients. Restricted to the mesh subnet.</p>","path":["Services","NFS"],"tags":[]},{"location":"services/nfs/#how","level":2,"title":"How","text":"<p>The NFS server role exports <code>/bulkdata/Media</code> with the following options:</p> <pre><code>nfs_exports:\n  - path: /bulkdata/Media\n    clients: \"10.0.0.0/24\"\n    options: \"all_squash,anonuid=0,anongid=451\"\n</code></pre> <p><code>all_squash</code> maps all client requests to the anonymous UID/GID. The anonymous GID (451) grants read access to the media dataset.</p>","path":["Services","NFS"],"tags":[]},{"location":"services/nfs/#why","level":2,"title":"Why","text":"<p>NFS is the simplest way to share files over the network. The mesh VPN means the export is only accessible to hosts on the overlay — no need for complex access controls.</p> <p><code>all_squash</code> is critical. Without it, the client's UID would need to match the server's UID for the media dataset. With <code>all_squash</code>, every client maps to the same anonymous user, regardless of their local UID.</p>","path":["Services","NFS"],"tags":[]},{"location":"services/toggleable-services/","level":1,"title":"Toggleable Services","text":"","path":["Services","Toggleable Services"],"tags":[]},{"location":"services/toggleable-services/#what","level":2,"title":"What","text":"<p>Jellyfin (media server) and Home Assistant (smart home) are deployed but disabled by default. A single flag enables them.</p>","path":["Services","Toggleable Services"],"tags":[]},{"location":"services/toggleable-services/#how","level":2,"title":"How","text":"<p>Each service has an <code>enabled</code> flag in its role defaults:</p> <pre><code># roles/jellyfin-container/defaults/main.yml\njellyfin_enabled: false\n\n# roles/homeassistant/defaults/main.yml\nhomeassistant_enabled: false\n</code></pre> <p>To enable, override in <code>group_vars</code>:</p> <pre><code>jellyfin_enabled: true\nhomeassistant_enabled: true\n</code></pre> <p>The playbook conditionally runs the role based on this flag.</p>","path":["Services","Toggleable Services"],"tags":[]},{"location":"services/toggleable-services/#why","level":2,"title":"Why","text":"<p>Not every service needs to run all the time. Jellyfin and Home Assistant are services I use occasionally — they don't need to consume resources 24/7.</p> <p>The toggle pattern means the roles are always present in the repo (tested, maintained, ready), but the services only run when explicitly enabled. This is different from removing the role entirely — the code stays current, and enabling the service is a one-line change.</p> <p>Home Assistant USB passthrough</p> <p>Home Assistant requires USB device passthrough (<code>/dev/ttyUSB0</code>) for Zigbee communication. This is configured in the quadlet only when <code>homeassistant_enabled: true</code> — the device passthrough directive is conditional.</p>","path":["Services","Toggleable Services"],"tags":[]},{"location":"storage/","level":1,"title":"Storage","text":"<p>Persistent data is the lifeblood of a homelab. I use ZFS for everything — snapshots, replication, and cloud backup.</p> <ul> <li>ZFS Datasets — workload profiles, per-service datasets</li> <li>Snapshots with Sanoid — policy-driven retention</li> <li>Replication with Syncoid — pull-based DR replication</li> <li>Cloud Backup — rclone to pCloud</li> </ul>","path":["Storage"],"tags":[]},{"location":"storage/cloud-backup/","level":1,"title":"Cloud Backup","text":"","path":["Storage","Cloud Backup"],"tags":[]},{"location":"storage/cloud-backup/#what","level":2,"title":"What","text":"<p>Rclone backs up data to pCloud. Each user has their own rclone instance with a separate system user.</p>","path":["Storage","Cloud Backup"],"tags":[]},{"location":"storage/cloud-backup/#how","level":2,"title":"How","text":"<p>Two rclone instances run as separate system users:</p> User Target <code>rclone-user-a</code> pCloud (user A) <code>rclone-user-b</code> pCloud (user B) <p>Each instance:</p> <ul> <li>Runs as a <code>nologin</code> system user (per ADR-0001)</li> <li>Has its own lingering systemd session</li> <li>Backs up on a 4-hour schedule via systemd user timer</li> <li>Is managed via <code>systemctl --machine=&lt;user&gt;@.host --user</code></li> </ul>","path":["Storage","Cloud Backup"],"tags":[]},{"location":"storage/cloud-backup/#why","level":2,"title":"Why","text":"<p>Separate rclone instances per user ensure that:</p> <ul> <li>Backup credentials for user A are not accessible to user B's containers</li> <li>Each user's backup schedule is independent</li> <li>If one user's backup fails, the other continues unaffected</li> </ul> <p>The same pattern applies as with containers: per-service isolation extends to backup processes. A compromise of a container running under user A cannot access user B's backup credentials or data.</p> <p>Why pCloud?</p> <p>pCloud offers lifetime plans with client-side encryption. For a homelab backup target, the cost-per-TB is competitive with S3, and there's no egress pricing. The tradeoff is slower restore times compared to a local or VPS-based backup target — which is why ZFS replication to the DR host is the primary backup, and pCloud is the secondary.</p>","path":["Storage","Cloud Backup"],"tags":[]},{"location":"storage/replication-syncoid/","level":1,"title":"Replication with Syncoid","text":"","path":["Storage","Replication with Syncoid"],"tags":[]},{"location":"storage/replication-syncoid/#what","level":2,"title":"What","text":"<p>Syncoid handles ZFS replication over SSH. I use it for disaster recovery — the DR host pulls snapshots from the home server every 2 hours.</p>","path":["Storage","Replication with Syncoid"],"tags":[]},{"location":"storage/replication-syncoid/#how","level":2,"title":"How","text":"<p>The replication is pull-based: the DR host (FreeBSD on RockPro64) initiates the pull, not the home server pushing.</p> <p>On the DR host, cron runs every 2 hours:</p> <pre><code>0 */2 * * * /usr/local/bin/syncoid --no-privilege-elevation --no-sync-snap --no-rollback -r zfs-send@100.100.100.4:bulkdata/backups zpool/backup\n</code></pre> <p>ZFS delegation enables non-root replication. Each side has a dedicated user with specific permissions:</p> Host User Permissions Dataset Home server <code>zfs-send</code> <code>snapshot,send,hold,destroy</code> <code>bulkdata/backups</code> DR host <code>zfs-recv</code> <code>create,receive,mount,rollback,destroy</code> <code>zpool/backup</code>","path":["Storage","Replication with Syncoid"],"tags":[]},{"location":"storage/replication-syncoid/#why","level":2,"title":"Why","text":"<p>Pull-based replication has a key advantage: the DR host controls when replication happens. If the home server is under load, the DR host can back off. If the home server is offline, the DR host retries on schedule.</p> <p>Push-based replication would require the home server to maintain a list of DR targets and manage SSH keys for each one. Pull-based means the DR host is self-contained — it knows where to pull from, and the home server doesn't need to know the DR host exists.</p> <p>Performance ceiling</p> <p>The Nebula tunnel between hosts maxes out at ~89 Mbps on the RockPro64's RK3399. This is a hardware limit — the RK3399's network stack cannot push more data through the tunnel. No software optimization can improve this.</p>","path":["Storage","Replication with Syncoid"],"tags":[]},{"location":"storage/snapshots-sanoid/","level":1,"title":"Snapshots with Sanoid","text":"","path":["Storage","Snapshots with Sanoid"],"tags":[]},{"location":"storage/snapshots-sanoid/#what","level":2,"title":"What","text":"<p>Sanoid provides policy-driven ZFS snapshot management. I use it to automatically create and prune snapshots on a schedule.</p>","path":["Storage","Snapshots with Sanoid"],"tags":[]},{"location":"storage/snapshots-sanoid/#how","level":2,"title":"How","text":"<p>Sanoid is deployed on both the home server and the DR host. Configuration is template-driven:</p> sanoid.conf<pre><code>[bulkdata/backups]\nuse_template = production\nrecursive = yes\n\n[template_production]\nfrequently = 0\nhourly = 24\ndaily = 7\nweekly = 4\nmonthly = 6\nyearly = 0\nautosnap = yes\nautoprune = yes\n</code></pre> <p>On FreeBSD (DR host), sanoid runs via cron every minute:</p> <pre><code>* * * * * sanoid --cron\n</code></pre> <p>On Debian (home server), a systemd timer handles scheduling.</p> <p>Sanoid is applied to:</p> <ul> <li><code>bulkdata/backups</code> — replicated backup data</li> <li><code>bulkdata/podman/forgejo</code> — self-hosted git data</li> </ul>","path":["Storage","Snapshots with Sanoid"],"tags":[]},{"location":"storage/snapshots-sanoid/#why","level":2,"title":"Why","text":"<p>Manual snapshot management doesn't scale. Without sanoid, snapshots accumulate until someone remembers to prune them — or they never get pruned at all.</p> <p>Sanoid's template system means I define retention once and apply it to every dataset. The same template produces hourly, daily, weekly, and monthly snapshots with automatic pruning. When I add a new dataset that needs snapshots, I apply the template and it inherits the entire policy.</p> <p>Why not zfs-auto-snapshot?</p> <p><code>zfs-auto-snapshot</code> is a simpler tool that creates snapshots on a fixed schedule. Sanoid adds template-based retention policies, which means I can say \"keep 24 hourly, 7 daily, 4 weekly\" instead of \"keep the last N snapshots.\" The policy is explicit, not count-based.</p>","path":["Storage","Snapshots with Sanoid"],"tags":[]},{"location":"storage/zfs-datasets/","level":1,"title":"ZFS Datasets","text":"","path":["Storage","ZFS Datasets"],"tags":[]},{"location":"storage/zfs-datasets/#what","level":2,"title":"What","text":"<p>Every service with persistent data gets its own ZFS dataset. Datasets are tuned to their workload profile — databases get small record sizes, media gets large ones.</p>","path":["Storage","ZFS Datasets"],"tags":[]},{"location":"storage/zfs-datasets/#how","level":2,"title":"How","text":"<p>Workload profiles are defined in <code>playbooks/group_vars/all.yml</code> and applied when creating datasets via <code>include_role: zfs-dataset</code>:</p> Profile recordsize compression atime xattr Use case <code>db</code> 16K lz4 off sa Databases, small random I/O <code>cache</code> 128K lz4 off sa General container data <code>media</code> 1M lz4 off sa Large sequential files <p>Dataset paths follow the pattern: <code>bulkdata/podman/&lt;service&gt;/</code></p> <pre><code>- name: Create ZFS dataset for myservice\n  include_role:\n    name: zfs-dataset\n  vars:\n    zfs_dataset_name: \"bulkdata/podman/myservice\"\n    zfs_profile: cache\n</code></pre>","path":["Storage","ZFS Datasets"],"tags":[]},{"location":"storage/zfs-datasets/#why","level":2,"title":"Why","text":"<p>ZFS datasets provide:</p> <ul> <li>Per-service snapshots — snapshot one service without affecting others</li> <li>Per-service quotas — prevent one service from consuming all disk</li> <li>Workload-tuned I/O — databases don't waste I/O on 1M record sizes</li> <li>Send/receive granularity — replicate individual datasets, not entire pools</li> </ul> <p>Without per-service datasets, a single <code>bulkdata/podman</code> dataset means all services share the same recordsize and compression settings. A database container and a media server would get the same I/O tuning — which is optimal for neither.</p>","path":["Storage","ZFS Datasets"],"tags":[]}]}